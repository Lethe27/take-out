<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        insert into orders (number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status,
                           amount, remark, phone, address, user_name, consignee, cancel_reason, rejection_reason,
                           cancel_time, estimated_delivery_time, delivery_status, delivery_time, pack_amount, tableware_number, tableware_status)
        values (#{number}, #{status}, #{userId}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod},
                #{payStatus}, #{amount}, #{remark}, #{phone}, #{address}, #{userName}, #{consignee}, #{cancelReason},
                #{rejectionReason}, #{cancelTime}, #{estimatedDeliveryTime}, #{deliveryStatus}, #{deliveryTime}, #{packAmount}, #{tablewareNumber}, #{tablewareStatus})

    </insert>
    <insert id="insertBatch">
        insert into order_detail(name, image, order_id, dish_id, setmeal_id, dish_flavor, amount)
        values
        <foreach collection="orderDetailList" item="item" separator=",">
            (#{item.name}, #{item.image}, #{item.orderId}, #{item.dishId},
             #{item.setmealId}, #{item.dishFlavor}, #{item.amount})
        </foreach>
    </insert>

    <update id="update" parameterType="com.sky.entity.Orders">
        update orders
        <set>
            <if test="cancelReason != null and cancelReason!='' "> cancel_reason=#{cancelReason}, </if>
            <if test="rejectionReason != null and rejectionReason!='' "> rejection_reason=#{rejectionReason}, </if>
            <if test="cancelTime != null"> cancel_time=#{cancelTime}, </if>
            <if test="payStatus != null"> pay_status=#{payStatus}, </if>
            <if test="payMethod != null"> pay_method=#{payMethod}, </if>
            <if test="checkoutTime != null"> checkout_time=#{checkoutTime}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="deliveryTime != null"> delivery_time = #{deliveryTime} </if>
        </set>
        where id = #{id}
    </update>
    <select id="pageQuery" resultType="com.sky.entity.Orders">
        select *
        from orders
        <where>
            <if test="status != null">
                status = #{status}
            </if>
            <if test="number != null and number != '' ">
                and number = #{number}
            </if>
            <if test="phone != null and number != '' ">
                and phone = #{phone}
            </if>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>
        </where>
        order by order_time desc
    </select>

    <select id="getDishesByOrderId" resultType="java.lang.String">
        select
            ifnull(group_concat(distinct d.name separator ','), '') as all_dish_names,
            count(distinct d.id) as dish_count  -- 统计去重后的菜品数量（用d.id更精准，避免名称重复）
        from order_detail od
                 left join setmeal_dish sd on od.setmeal_id = sd.setmeal_id
                 join dish d on (od.dish_id = d.id or sd.dish_id = d.id)
        where od.order_id = #{orderId}
          and (od.dish_id is not null or sd.dish_id is not null);
    </select>
    <select id="countByStatus" resultType="java.lang.Integer">
        select count(*) from orders
        where status = #{status}
    </select>

    <select id="findOverTimeOrders" resultType="com.sky.entity.Orders">
        select * from orders
        where status = #{status}
          and order_time &lt;= #{overTime}
    </select>
</mapper>
